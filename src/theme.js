import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#ffd54f",
      main: "#ffd54f",
      dark: "#ffd54f",
      contrastText: "#ffffff",
    },
    secondary: {
      light: "#5e5e5e",
      main: "#5e5e5e",
      dark: "#5e5e5e",
      contrastText: "#000000",
    },
  },
});

export default theme;
